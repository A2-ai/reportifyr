% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/source_meta.R
\name{shiny_source}
\alias{shiny_source}
\title{Build a structured \code{source} value for an artifact produced from a Shiny app}
\usage{
shiny_source(app_name, app_version = NULL)
}
\arguments{
\item{app_name}{Character scalar. Name of the installed R package that
is the Shiny app.}

\item{app_version}{Optional character scalar version. When \code{NULL}
(default) the version is resolved via \code{utils::packageVersion()},
which requires the app to be installed as a package.}
}
\value{
A named list with \code{app_name} and \code{app_version} elements,
classed as \code{c("shiny_source", "rpfy_source_meta")}.
}
\description{
Returns a classed list suitable for the \code{origin}
argument of \code{\link[=rpfy_context]{rpfy_context()}}. The \code{type} discriminator is not
stored on the list; it is emitted at serialization time so the
class is the single source of truth.
}
\examples{
\dontrun{
ctx <- rpfy_context(origin = shiny_source("myapp"))
ggsave_with_metadata(
  filename = "OUTPUTS/figures/plot.png",
  context = ctx
)
}
}
