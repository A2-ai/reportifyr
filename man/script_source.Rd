% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/source_meta.R
\name{script_source}
\alias{script_source}
\title{Build a structured \code{source} value for an artifact produced from a script}
\usage{
script_source(path, creation_author, latest_author, creation_time, latest_time)
}
\arguments{
\item{path}{Character scalar. Path to the source script, relative to
the project root.}

\item{creation_author}{Character scalar. Git author of the first commit
touching the script.}

\item{latest_author}{Character scalar. Git author of the most recent
commit touching the script.}

\item{creation_time}{Character scalar. Timestamp of the first commit.}

\item{latest_time}{Character scalar. Timestamp of the most recent commit.}
}
\value{
A named list with \code{creation_author}, \code{latest_author}, \code{path},
\code{creation_time}, and \code{latest_time} elements, classed as
\code{c("script_source", "rpfy_source_meta")}.
}
\description{
Returns a classed list suitable for the \code{origin}
argument of \code{\link[=rpfy_context]{rpfy_context()}}. Typically produced internally by
\code{\link[=rpfy_context]{rpfy_context()}} via auto-detection; exposed for callers that want
to stamp a known script identity. The \code{type} discriminator is not
stored on the list; it is emitted at serialization time so the class
is the single source of truth.
}
\examples{
\dontrun{
src <- script_source(
  path            = "scripts/01-pk.R",
  creation_author = "jake",
  latest_author   = "jake",
  creation_time   = "2026-01-01 00:00:00",
  latest_time     = "2026-04-20 12:00:00"
)
ctx <- rpfy_context(origin = src)
}
}
