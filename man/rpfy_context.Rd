% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rpfy_context.R
\name{rpfy_context}
\alias{rpfy_context}
\title{Build a reportifyr metadata context}
\usage{
rpfy_context(origin = NULL, addl_metadata = NULL)
}
\arguments{
\item{origin}{Controls what is recorded in \code{source_meta} in the
artifact JSON. One of:
\itemize{
\item \code{NULL} (default) — auto-detect via \code{this.path} + git lookup.
\item A character scalar — free-form text written as
\code{source_meta$text}.
\item A named list — written verbatim, e.g., the structured
output of \code{\link[=shiny_source]{shiny_source()}}.
}}

\item{addl_metadata}{Optional named list of caller-supplied
provenance tags, written under the \code{addl_meta} key in the JSON.
Values must be scalar character, numeric, or logical. Purely
informational; never rendered into the report footer.}
}
\value{
An object of class \code{rpfy_context} — a list with fields
\code{system_meta}, \code{source_meta}, \code{addl_metadata}, \code{author},
\code{project_root}.
}
\description{
Captures session-level metadata once so that it can be
reused across many \verb{*_with_metadata()} wrapper calls without
recomputing on every artifact. Construct the context at the top
of a script or Shiny app and pass it via the \code{context} argument
to \code{\link[=ggsave_with_metadata]{ggsave_with_metadata()}}, \code{\link[=save_rds_with_metadata]{save_rds_with_metadata()}},
\code{\link[=write_csv_with_metadata]{write_csv_with_metadata()}}, or \code{\link[=write_object_metadata]{write_object_metadata()}}.
}
\examples{
\dontrun{
ctx <- rpfy_context(
  origin = shiny_source("myapp"),
  addl_metadata = list(analyst = "jake", study = "PK-001")
)

print(ctx)
validate(ctx)

ggsave_with_metadata("plot.png", context = ctx, meta_type = "efficacy")
}
}
