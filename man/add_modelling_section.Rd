% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/build_reviewer_guide.R
\name{add_modelling_section}
\alias{add_modelling_section}
\title{Add a modelling section to an existing reviewer guide document}
\usage{
add_modelling_section(
  reviewer_guide_in,
  reviewer_guide_out = NULL,
  control_streams,
  section_title = "Modelling",
  descriptions = NULL
)
}
\arguments{
\item{reviewer_guide_in}{The file path to the input reviewer guide \code{.docx} file.}

\item{reviewer_guide_out}{The file path to the output \code{.docx} file to save to. Default is \code{NULL} (saves to same directory with "_modelling" suffix).}

\item{control_streams}{A character vector of file paths to NONMEM control stream files (.mod or .ctl).}

\item{section_title}{The title for the modelling section. Default is "Modelling".}

\item{descriptions}{Either a named list where names are control stream basenames and values are descriptions, or a single string description (for single control stream).}
}
\value{
Invisibly returns the modelling data frame that was added to the document.
}
\description{
Takes a list of NONMEM control streams, processes them through get_mod_io_paths,
creates a formatted flextable, and adds it as a new "Modelling" section to an existing reviewer guide document.
}
\examples{
\dontrun{
# Add modelling section with multiple control streams
add_modelling_section(
  reviewer_guide_in = "reviewer_guide.docx",
  control_streams = c("model1.mod", "model2.mod"),
  descriptions = list(
    "model1.mod" = "Base population PK model",
    "model2.mod" = "Final covariate model"
  )
)

# Add modelling section with single control stream
add_modelling_section(
  reviewer_guide_in = "reviewer_guide.docx",
  control_streams = "final_model.mod",
  descriptions = "Final population PK model"
)
}
}
